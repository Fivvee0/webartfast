For Windows:
Run in cmd: run.bat

For Linux/MacOS users:
Run in shell: chmod +x run.sh
Run in shell: ./run.sh

For Docker build:
Run in cmd/shell: docker build -t name .

For Docker run:
Run in cmd/shell: docker run -p 80:80 name